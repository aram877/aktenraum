# Session — 2026-06-22

## Focus / scope

First-time bring-up of the full stack on a new machine (MacBook Pro M5 Pro, 64 GB), plus two code changes needed to make it run locally: migrating the RAG embedding model bge-m3 → Qwen3-Embedding-4B, and repointing the LLM quality-tier mapping off non-existent `gemma4:*` models onto the locally-pulled qwen2.5 instruct model.

## Repo state

- Branch: `main`
- Tests: **NOT run this session** — the bring-up machine has no `uv`/`pytest` installed (runtime-only checkout). All edited files pass `python3 -m py_compile`, and affected test files were updated in lockstep, but the suite must be run (`task test:py`) before any of this is committed.
- **Nothing committed.** Two code changes below are live in the running containers but uncommitted in the working tree.

## What shipped

### RAG — embedding model migration (bge-m3 → Qwen3-Embedding-4B)

- Collapsed the dense dimension from five hardcoded `1024` literals to a single source of truth: new `DENSE_DIM = 2560` constant in `packages/aktenraum-core/src/aktenraum_core/rag/embedder.py`, exported via `rag/__init__.py`, imported as `QdrantVectorStore`'s default `dense_dim`. The three call sites (`auto-tagger/main.py`, `aktenraum-api/main.py`, `eval/runner.py`) dropped their explicit `dense_dim=1024` and now inherit the constant.
- `OllamaEmbedder` default model `bge-m3` → `qwen3-embedding:4b`; both services' `embedding_model` config defaults + both `*.env.example` files updated to `qwen3-embedding:4b` (with comments tying the dim to the model).
- Updated tests pinning the old values (`test_rag_embedder`, `test_rag_vector_store`, `test_ai_retrieval`, `test_rag_backfill`, `test_indexer`, `test_eval_runner`) — `1024`→`2560`, `bge-m3`→`qwen3-embedding:4b`.
- Operator-facing comments made model-neutral; chunker doc-comments left (its 8k-context assumption still holds — qwen3 is 32k).

### Settings — LLM quality-tier mapping fix

- `services/aktenraum-api/src/aktenraum_api/settings/quality.py`: `QUALITY_TO_MODEL` repointed from non-existent `gemma4:26b` / `gemma4:e4b` to `qwen2.5:14b-instruct-q8_0` (both tiers, since it's the only instruct model pulled on this host). This is the actual source of truth for the live model — `OLLAMA_MODEL` env is only a fallback when the api is unreachable.
- Updated `test_settings_router.py` assertions and the stale `gemma4` comments in `schemas.py` / `db/models.py`.

### Ops — first-time deployment on the Mac

- `~/aktenraum` data dirs created; secrets generated; `AKTENRAUM_DATA_DIR=/Users/akeushgerian/aktenraum`, `LLM_BACKEND=ollama`, `OLLAMA_MODEL=qwen2.5:14b-instruct-q8_0` set across the env files.
- Pulled `qwen3-embedding:4b` (2.5 GB) + `qwen2.5:14b-instruct-q8_0` (15 GB).
- `task setup` built the four local images, started all 10 containers, minted the Paperless token, bootstrapped 12 `ai_*` custom fields + 10 `ai-*` tags, inited restic + first snapshot.
- Recreated `auto-tagger` + `aktenraum-api` post-setup so they loaded the API token (fix-token writes it after `compose up`).
- Verified end-to-end: health green, SPA serves, token authenticates, Qdrant collection created at **2560-dim** (confirms the migration), one document (Anthropic/Claude Pro invoice) classified → propagated → RAG-indexed.

## Diagnosed but not fixed (working as designed)

- **`task setup` exits non-zero on first run** — benign `restic` exit 3: it can't read Paperless's live lock/log files during the hot first snapshot. Snapshot still saves; real data captured.
- **Login "Ungültige Anmeldedaten"** — not a bug; a mistyped generated password (contains `0`/`1`). Credentials verified correct against the API.
- **Ask: "wie viel für claude *code*?" → nicht gefunden, but "claude" works** — correct faithful-RAG behavior. The invoice is for **Claude Pro** (€21.42); the string "Code" appears nowhere in the OCR, so the answer model rightly refuses to attribute the amount to a product not in the document. Precision-over-recall at the generation step; no change warranted.

## Pick up next session

- **Run `task test:py` and commit** the two code changes (embedding migration + quality-mapping fix). Both are uncommitted and unverified by the suite. Likely one commit each, with this session note + a short note on the embedding-model decision.
- **CLAUDE.md is stale on two points** that bit this session — worth updating in the same commit: (1) the recommended `OLLAMA_MODEL`/embedding defaults still say bge-m3 / qwen2.5:32b; (2) nothing documents that the live model comes from the `app_settings` quality-tier map, not `OLLAMA_MODEL`. Add a Known-gotcha row.
- **`QUALITY_TO_MODEL` both tiers = 14b** is a stopgap. When a larger instruct model is pulled (e.g. `qwen2.5:32b-instruct-q8_0`), wire `high` → it so the Settings → Qualität toggle is meaningful again.
- Consider an OpenSpec change to make `EMBEDDING_MODEL` + its dimension a single configured pair (env-driven dim) so future embedder swaps don't need a code edit.

## Roadmap progress

- **rag-phase-1**: embedding model upgraded bge-m3 → Qwen3-Embedding-4B (2560-dim). Sub-phase 1.11 (model auto-pull) still pending — models were pulled manually this session. No eval comparison run yet (corpus is a single document).

---

## Follow-up (committed 2026-06-23)

- Code changes (embedding migration + quality-mapping fix + session note) were committed by the maintainer in `0459848 "refactor: change embedding model"` and pushed to `origin/main`. That commit did **not** include CLAUDE.md.
- **Tests now run and pass**: `uv` installed via brew on the Mac, `uv sync`, then **647 passed, ruff clean** (58.97s) — retroactively satisfies the never-commit-before-tests rule for `0459848`.
- This follow-up commit adds: `task rag:reembed` (drop Qdrant collection + `backfill --force`, container-internal, cross-platform) and the **CLAUDE.md catch-up** the prior commit skipped — Known-gotcha rows for (1) live model = DB quality-tier map not `OLLAMA_MODEL`, (2) embedding-model change requires `task rag:reembed`; plus the embedding/model config rows and RAG status updated off bge-m3.
- Bug-fix-confirmation (quality mapping): verified live — doc #2 (Anthropic/Claude Pro invoice) classified cleanly with the 14b after the fix; maintainer authorized the commit.
